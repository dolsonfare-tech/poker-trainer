{
  "name": "CheckRaise",
  "version": "1.0.0",
  "description": "AI-powered Texas Hold'em skill trainer",
  "author": "David Olson",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@sentry/react": "^10.64.0",
    "@supabase/supabase-js": "^2.110.0",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^13.5.0",
    "posthog-js": "^1.396.6",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "audit:scenarios": "node scripts/audit-scenarios.mjs && node scripts/gen-scenario-index.mjs --check",
    "audit:observations": "node scripts/audit-observations.mjs",
    "eval:coach": "node scripts/eval-coach.mjs",
    "check:invariants": "node scripts/check-invariants.mjs",
    "gates": "npm run check:invariants && npm run audit:scenarios && npm run audit:observations && CI=true npm test && npm run simulate:schemas && npm run playtest:personas && CI=true npm run build && npm run check:bundle",
    "export:review": "node scripts/export-review.mjs",
    "simulate:schemas": "node scripts/simulate-schemas.mjs",
    "playtest:personas": "node scripts/playtest-personas.mjs",
    "e2e": "node e2e/run.mjs",
    "e2e:build": "REACT_APP_SUPABASE_URL= REACT_APP_SUPABASE_ANON_KEY= REACT_APP_POSTHOG_KEY= react-scripts build",
    "eject": "react-scripts eject",
    "gen:scenario-index": "node scripts/gen-scenario-index.mjs",
    "check:bundle": "node scripts/check-bundle.mjs"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}